spring:
  application:
    name: WhatToEat

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/what_to_eat?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true